# ASR (venv-asr, NumPy 1.26)
# WhisperX + torch 2.5.1 + pyannote for VAD
# NOTE: Forced NumPy 1.26.4 to fix "AttributeError: np.NaN" and ensure compatibility

numpy==1.26.4
torch==2.5.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121
torchaudio==2.5.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121

# WhisperX
whisperx==3.1.1
faster-whisper==1.0.3

# VAD dependencies
# We let pip resolve sub-dependencies (core, database, metrics) 
# but constrain them to work with our pinned NumPy if needed.
pyannote.audio==3.1.1

# Utilities
huggingface_hub>=0.36.0
transformers>=4.0.0
docopt>=0.6.2
