# 🔄 Обновление кода на сервере

Инструкция по обновлению проекта на сервере после изменений в Git.

## ⚠️ Важно понимать:

### Git содержит:
- ✅ Исходный код (.py файлы)
- ✅ requirements.txt (список зависимостей)
- ✅ Конфигурационные файлы
- ✅ Скрипты установки

### Git НЕ содержит:
- ❌ venv/ (виртуальное окружение)
- ❌ Установленные Python пакеты
- ❌ Скачанные модели (Whisper, NLLB, Ollama)
- ❌ Файлы в temp/ и output/

## 🔄 Обновление кода (простое)

Если изменили только `.py` файлы (без изменения зависимостей):

```bash
# На сервере
cd /opt/youtube-shorts-generator

# Остановить сервер
pkill -f "backend.main"

# Обновить код
git pull origin main

# Запустить
source venv/bin/activate
export PYTHONPATH=/opt/youtube-shorts-generator
nohup python -m backend.main > server.log 2>&1 &
```

**Зависимости не переустанавливаются** - используются существующие в venv/

---

## 🔧 Обновление с переустановкой зависимостей (если изменили requirements.txt)

```bash
# На сервере
cd /opt/youtube-shorts-generator

# Остановить сервер
pkill -f "backend.main"

# Сохранить видео и модели
mkdir -p /tmp/backup
cp -r temp/*.mp4 /tmp/backup/ 2>/dev/null || true

# Обновить код
git pull origin main

# Переустановить зависимости
source venv/bin/activate

# Установить новые зависимости
pip install --upgrade pip

# NumPy
pip install "numpy>=1.23.0,<2.0.0"

# PyTorch
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118

# Остальное
pip install -r requirements.txt

# Восстановить видео
cp /tmp/backup/*.mp4 temp/ 2>/dev/null || true

# Запустить
export PYTHONPATH=/opt/youtube-shorts-generator
nohup python -m backend.main > server.log 2>&1 &
```

---

## 🆕 Полная переустановка (если что-то сломалось)

```bash
# На сервере
cd /opt/youtube-shorts-generator

# Остановить всё
pkill -f "backend.main"
pkill -f "ollama serve"

# Сохранить важное
mkdir -p /tmp/backup
cp -r temp/*.mp4 /tmp/backup/ 2>/dev/null || true
cp .env /tmp/backup/.env 2>/dev/null || true

# Удалить venv
rm -rf venv

# Обновить код
git pull origin main

# Запустить установку заново
./install.sh

# Восстановить файлы
cp /tmp/backup/*.mp4 temp/ 2>/dev/null || true
cp /tmp/backup/.env .env 2>/dev/null || true

# Запустить
./run.sh
```

---

## 📋 Что нужно делать при разных изменениях:

| Что изменили | Нужно переустанавливать? | Команды |
|--------------|-------------------------|---------|
| Только .py файлы | ❌ Нет | `git pull` + restart |
| requirements.txt | ✅ Да | `git pull` + `pip install` |
| Конфигурация | ❌ Нет | `git pull` + restart |
| Скрипты (.sh) | ❌ Нет | `git pull` + `chmod +x` |

---

## 🎯 Для текущего обновления (добавили локальные файлы):

```bash
# На сервере (после git push на Mac)

cd /opt/youtube-shorts-generator

# Остановить
pkill -f "backend.main"

# Обновить
git pull origin main

# Зависимости НЕ меняли - переустанавливать не нужно!
# Просто запустить

source venv/bin/activate
export PYTHONPATH=/opt/youtube-shorts-generator
nohup python -m backend.main > server.log 2>&1 &

sleep 3
curl http://localhost:8000/health
```

---

## ✅ Итого:

**Сейчас зависимости на сервере уже правильные** (torch 2.2.2+, faster-whisper, etc).

После `git pull`:
- ✅ Обновится **код** (новый endpoint для локальных файлов)
- ✅ Зависимости **останутся** как есть (в venv/)
- ✅ Видео в temp/ **останутся**

Просто `git pull` + restart - и всё!

---

**Заливайте на GitHub!** После этого дам команды для обновления сервера. 🚀
