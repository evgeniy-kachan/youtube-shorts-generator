# ⚡ Quick Start Guide

Быстрый старт для YouTube Shorts Generator.

## 🚀 За 5 минут

### Предварительные требования

- Ubuntu 22.04 с NVIDIA GPU (A4000 или аналог)
- CUDA 11.8+ установлена
- Python 3.10+
- 32GB+ RAM
- 100GB+ свободного места

### Установка и запуск

```bash
# 1. Клонируйте/перейдите в директорию проекта
cd /path/to/project_blog

# 2. Запустите автоматическую установку
chmod +x install.sh
./install.sh

# 3. Запустите сервер
./run.sh
```

Откройте браузер: `http://localhost:8000`

## 📝 Пошаговое использование

### Через Web UI

1. Вставьте YouTube URL
2. Дождитесь анализа (~15-20 мин для часового видео)
3. Выберите интересные сегменты
4. Выберите метод конвертации в вертикальный формат:
   - 🌟 **Размытый фон** (рекомендуется) - популярный стиль для Shorts
   - ✂️ **Обрезка по центру** - быстрая обрезка
   - 🤖 **Умная обрезка** - с детекцией важных объектов
5. Нажмите "Создать клипы"
6. Скачайте готовые вертикальные клипы (1080×1920)

### Через API

```python
import requests
import time

# 1. Начать анализ
response = requests.post(
    "http://localhost:8000/api/video/analyze",
    json={"youtube_url": "https://www.youtube.com/watch?v=VIDEO_ID"}
)
task_id = response.json()["task_id"]

# 2. Ждать завершения
while True:
    status = requests.get(f"http://localhost:8000/api/video/task/{task_id}").json()
    if status["status"] == "completed":
        result = status["result"]
        break
    print(f"Progress: {status['progress']*100:.0f}%")
    time.sleep(5)

# 3. Обработать сегменты
video_id = result["video_id"]
segment_ids = [s["id"] for s in result["segments"][:3]]  # Top 3

response = requests.post(
    "http://localhost:8000/api/video/process",
    json={"video_id": video_id, "segment_ids": segment_ids}
)
process_task_id = response.json()["task_id"]

# 4. Ждать обработки
while True:
    status = requests.get(f"http://localhost:8000/api/video/task/{process_task_id}").json()
    if status["status"] == "completed":
        break
    time.sleep(5)

# 5. Скачать клипы
for segment_id in segment_ids:
    url = f"http://localhost:8000/api/video/download/{video_id}/{segment_id}"
    # Скачать файл...
```

## 🎯 Рекомендуемые настройки

### Для NVIDIA A4000 (16GB VRAM)

```env
# .env файл
DEEPSEEK_MODEL=deepseek-reasoner   # или deepseek-chat для экономии
WHISPER_MODEL=large-v3             # или medium для экономии памяти
WHISPER_COMPUTE_TYPE=float16       # или int8 для экономии памяти
```

### Если не хватает VRAM

В `backend/config.py`:
```python
WHISPER_MODEL = "medium"       # вместо large-v3
WHISPER_COMPUTE_TYPE = "int8"  # вместо float16
```

Используйте `DEEPSEEK_MODEL=deepseek-chat`, если нужно снизить стоимость/латентность.

## 🔧 Проверка установки

```bash
# Проверьте GPU
nvidia-smi

# Проверьте CUDA
nvcc --version

# Проверьте Python
python3 --version

# Проверьте FFmpeg
ffmpeg -version

# Проверьте backend
curl http://localhost:8000/health
```

## 📊 Производительность

| Видео | Транскрипция | Анализ | Обработка 1 клипа | Итого |
|-------|--------------|--------|-------------------|-------|
| 30 мин | 5 мин | 3 мин | 30 сек | ~10 мин |
| 1 час | 10 мин | 7 мин | 30 сек | ~20 мин |
| 2 часа | 20 мин | 15 мин | 30 сек | ~40 мин |

## 🐛 Типичные проблемы

### "CUDA out of memory"
```bash
# Уменьшите размер моделей
export WHISPER_MODEL=medium
export WHISPER_COMPUTE_TYPE=int8
```

### Ошибки DeepSeek API
- Проверьте `DEEPSEEK_API_KEY` в `.env`
- Убедитесь, что есть доступ к `https://api.deepseek.com`
- Снизьте размер батча (`DEEPSEEK_TRANSLATION_CHUNK_SIZE`)

### "FFmpeg not found"
```bash
sudo apt install ffmpeg
```

### Медленная работа
```bash
# Проверьте что используется GPU
nvidia-smi

# Проверьте загрузку
watch -n 1 nvidia-smi
```

## 📚 Дополнительная документация

- **README.md** - Полное описание проекта
- **DEPLOYMENT.md** - Детальное руководство по развертыванию
- **API_EXAMPLES.md** - Примеры использования API

## 🎓 Примеры видео для тестирования

Хорошо подходят для тестирования:
- Интервью (Joe Rogan, Lex Fridman)
- Образовательный контент
- Подкасты
- Лекции
- Обзоры

**Избегайте:**
- Музыкальные клипы (мало речи)
- Видео с несколькими языками
- Видео с плохим звуком

## 💡 Советы

1. **Начните с коротких видео** (10-20 минут) для тестирования
2. **Проверьте GPU загрузку** во время работы
3. **Регулярно очищайте** temp и output директории
4. **Используйте SSD** для temp директории
5. **Мониторьте память** при обработке длинных видео

## 🔄 Обновление

```bash
cd /path/to/project_blog
git pull
source venv/bin/activate
pip install -r requirements.txt --upgrade
cd frontend && npm install && cd ..
```

## 📞 Получить помощь

1. Проверьте логи:
   ```bash
   tail -f logs/app.log  # если настроено
   # или
   journalctl -u youtube-shorts -f  # если через systemd
   ```

2. Проверьте GitHub Issues

3. Создайте новый Issue с:
   - Описанием проблемы
   - Логами
   - Версией Python, CUDA, GPU
   - Шагами для воспроизведения

## ✅ Checklist первого запуска

- [ ] GPU работает (`nvidia-smi`)
- [ ] CUDA установлена (`nvcc --version`)
- [ ] Python 3.10+ установлен
- [ ] FFmpeg установлен
- [ ] DeepSeek API key настроен в .env
- [ ] Выполнен `./install.sh`
- [ ] Загружена LLM модель
- [ ] Сервер запустился (`./run.sh`)
- [ ] Браузер открывает UI
- [ ] `/health` endpoint отвечает

---

**Готово! Создавайте вирусные Shorts!** 🎬🚀

