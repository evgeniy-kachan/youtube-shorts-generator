# 📱 Конвертация в вертикальный формат (9:16)

Подробное описание конвертации горизонтальных YouTube видео в вертикальный формат для Reels/Shorts.

## 🎯 Целевой формат

- **Разрешение**: 1080×1920 пикселей
- **Соотношение сторон**: 9:16
- **Кодек**: H.264 (libx264)
- **Качество**: CRF 23 (высокое)

## 🔄 Методы конвертации

> ℹ️ Актуальные режимы: **letterbox** (чёрные поля без обрезки) и **center_crop**. Старые режимы blur_background/smart_crop удалены.

### 1. Чёрные поля (letterbox) ⚫️

**Описание:**
- Видео вписывается в 1080×1920 без обрезки
- Сверху и снизу добавляются чёрные поля
- Позволяет сохранить весь кадр

**Визуально:**
```
┌─────────────────┐
│█████████████████│ ← Чёрные поля
│░░░░VIDEO░░░░░░░│
│█████████████████│
└─────────────────┘
    1080×1920
```

**Плюсы:**
- Ничего не теряем из исходного кадра
- Смотрится аккуратно и контрастно
- Быстрая обработка

**Минусы:**
- Видимы полосы сверху/снизу

**Используйте для:**
- Интервью
- Подкасты
- Образовательный контент
- Когда важно показать весь слайд/кадр

**FFmpeg команда:**
```bash
ffmpeg -i input.mp4 \
  -vf "scale=1080:1920:force_original_aspect_ratio=decrease,\
       pad=1080:1920:(ow-iw)/2:(oh-ih)/2:black" \
  output.mp4
```

---

### 2. Обрезка по центру (center_crop) ✂️

**Описание:**
- Простая обрезка центральной части
- Быстрая обработка
- Без размытия

**Визуально:**
```
┌─────────────────────────────┐
│         ОБРЕЗАНО            │
│  ┌─────────────────┐        │
│  │                 │        │
│  │  ВИДИМАЯ ЧАСТЬ  │        │
│  │                 │        │
│  └─────────────────┘        │
│         ОБРЕЗАНО            │
└─────────────────────────────┘
        1080×1920
```

**Плюсы:**
- Быстрая обработка
- Простота
- Фокус на центре

**Минусы:**
- Теряются края кадра
- Может обрезать важные элементы
- Не всегда подходит

**Используйте для:**
- Видео где действие в центре
- Лекции (спикер в центре)
- Простой контент

**FFmpeg команда:**
```bash
ffmpeg -i input.mp4 \
  -vf "scale=-1:1920,crop=1080:1920" \
  output.mp4
```

---

### 3. Умная обрезка (smart_crop) — отключена

Режим smart_crop временно убран из интерфейса. Если он снова понадобится, потребуется отдельная сборка FFmpeg с OpenCV и детект лиц. Пока используйте letterbox или center_crop.

---

## 🎨 Сравнение методов

| Метод | Скорость | Качество | Потеря контента | Использование |
|-------|----------|----------|-----------------|---------------|
| **Чёрные поля (letterbox)** | Быстрая | ⭐⭐⭐⭐⭐ | 0% | 🔥 По умолчанию |
| **Обрезка (center_crop)** | Самая быстрая | ⭐⭐⭐ | ~40% | Когда нужен «зум» |

## 🔧 Настройка

### В конфигурации (backend/config.py):

```python
VERTICAL_CONVERSION_METHOD = "letterbox"  # По умолчанию
TARGET_WIDTH = 1080
TARGET_HEIGHT = 1920
```

### Через API:

```python
import requests

requests.post('http://localhost:8000/api/video/process', json={
    'video_id': 'VIDEO_ID',
    'segment_ids': ['segment_0', 'segment_1'],
    'vertical_method': 'letterbox'  # или center_crop
})
```

### Через Web UI:

Выпадающий список на этапе выбора сегментов:
- ⚫️ Чёрные поля (по умолчанию)
- ✂️ Обрезка по центру

## 📊 Производительность

На NVIDIA A4000:

| Метод | Время для 1 минуты видео |
|-------|-------------------------|
| Чёрные поля | ~4-5 секунд |
| Обрезка | ~2-3 секунды |

## 💡 Рекомендации

### Для разных типов контента:

**Интервью / Подкасты:**
```python
vertical_method = "letterbox"  # ✅ Лучший выбор
```
- Весь контент виден
- Профессиональный вид
- Популярно в TikTok/Instagram

**Образовательный контент:**
```python
vertical_method = "letterbox"  # ✅ Лучший выбор
```
- Важные элементы на экране
- Презентации/диаграммы видны полностью

**Летсплеи / Геймплей:**
```python
vertical_method = "center_crop"  # ✅ Подходит
```
- Игровое действие обычно в центре
- Быстрая обработка
- Минимальная потеря

**Влоги / Личные видео:**
```python
vertical_method = "letterbox"  # ✅ Универсальный
```
- Красивая картинка
- Подходит для любого контента

## 🔍 Технические детали

### Алгоритм "Чёрные поля (letterbox)":

1. **Масштабировать:**
   ```
   scale → вписать в 1080×1920 с сохранением пропорций
   ```

2. **Добавить поля:**
   ```
   pad → расширить кадр до точных размеров, x/y = (ow-iw)/2, (oh-ih)/2
   color → black
   ```

### Параметры качества:

```python
{
    'c:v': 'libx264',      # Кодек H.264
    'preset': 'medium',    # Баланс скорость/качество
    'crf': 23,             # Качество (18-28, меньше=лучше)
    'c:a': 'aac',          # Аудио кодек
    'b:a': '192k'          # Аудио битрейт
}
```

## 🎯 Примеры использования

### Python скрипт:

```python
from backend.services.video_processor import VideoProcessor
from pathlib import Path

processor = VideoProcessor(output_dir=Path('./output'))

# Чёрные поля
processor.convert_to_vertical(
    video_path='input.mp4',
    output_path='output_letterbox.mp4',
    method='letterbox'
)

# Обрезка
processor.convert_to_vertical(
    video_path='input.mp4',
    output_path='output_crop.mp4',
    method='center_crop'
)
```

### Через API:

```bash
# 1. Анализ
curl -X POST http://localhost:8000/api/video/analyze \
  -H "Content-Type: application/json" \
  -d '{"youtube_url": "https://www.youtube.com/watch?v=VIDEO_ID"}'

# 2. Обработка с letterbox
curl -X POST http://localhost:8000/api/video/process \
  -H "Content-Type: application/json" \
  -d '{
    "video_id": "VIDEO_ID",
    "segment_ids": ["segment_0"],
    "vertical_method": "letterbox"
  }'
```

## 🐛 Troubleshooting

### Проблема: Видео слишком сильно размыто

**Решение:**
Letterbox уже не размывает фон. При необходимости увеличьте/уменьшите поля через параметры pad в `video_processor.py`.

### Проблема: Обрезка режет важные части

**Решение:**
Используйте `letterbox` вместо `center_crop`:

```python
vertical_method = "letterbox"
```

### Проблема: Медленная обработка

**Решение:**
1. Используйте `center_crop` (самый быстрый)
2. Измените preset:
   ```python
   'preset': 'fast'  # вместо 'medium'
   ```

## 📚 Дополнительно

### Кастомные размеры:

Если нужны другие размеры (например, для Stories):

```python
# В backend/config.py
TARGET_WIDTH = 1080   # Stories: 1080
TARGET_HEIGHT = 1920  # Stories: 1920

# Для YouTube Shorts (то же самое):
TARGET_WIDTH = 1080
TARGET_HEIGHT = 1920
```

### Добавление водяных знаков:

Можно добавить в `video_processor.py`:

```python
.filter('drawtext', 
    text='@username',
    x='(w-text_w)/2',
    y='h-th-10',
    fontsize=24,
    fontcolor='white'
)
```

---

**Готовые вертикальные клипы идеально подходят для:**
- TikTok
- Instagram Reels
- YouTube Shorts
- Facebook Reels
- Snapchat Spotlight

🎬 Создавайте вирусный контент!

